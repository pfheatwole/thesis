.. ::

   This is separate because the HTML and Latex builders handle appendices
   differently. For the moment, the HTML builder treats appendices as
   unnumbered chapters, whereas the Latex builder uses alphabetical
   sequences via the `\appendix` command.

.. toctree::
   :maxdepth: 3

   content/symbols
   content/appendix
